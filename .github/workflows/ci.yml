name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  ci:
    uses: OpenActuarial/.github/.github/workflows/reusable-ci.yml@v1
    with:
      package: extremeloss
      coverage-min: 91
      extras: "dev,plot"
